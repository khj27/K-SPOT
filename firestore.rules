rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // K-SPOT은 서버의 Firebase Admin SDK를 통해서만 Firestore에 접근합니다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
