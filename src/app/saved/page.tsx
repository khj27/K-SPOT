import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function SavedPage() {
  return <RoutePlaceholder eyebrow="SAVED" title="저장한 장소와 여행" description="찜한 장소와 브라우저에 저장한 여행 일정을 모아볼 화면입니다." nextStep="STEP 7에서 localStorage 저장소와 빈 상태를 연결합니다." />;
}
