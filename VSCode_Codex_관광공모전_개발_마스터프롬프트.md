# VS Code Codex용 관광데이터 공모전 개발 마스터 프롬프트

아래 `시작 프롬프트` 전체를 VS Code Codex의 새 채팅에 붙여 넣고, 기존 UI 이미지도 같은 채팅에 첨부한다. 가능하면 프로젝트 루트에서 실행한다.

---

## 시작 프롬프트

당신은 이 프로젝트의 시니어 풀스택 개발자이자 제품 설계자다. 아래 요구사항을 기준으로 공모전 제출이 가능한 PC 중심 반응형 웹 서비스를 실제로 구현하라. 단순한 화면 시안이나 정적 목업에서 끝내지 말고, 핵심 사용자 흐름과 데이터 연동이 작동하는 MVP를 완성해야 한다.

### 1. 프로젝트 목표

- 공모전: 2026 관광데이터 활용 공모전 `②-2 웹·앱 구현 부문`
- 선택 지정과제: `3번 - K-콘텐츠 및 아티스트 관련 관광 인프라의 수도권 편중 현상 및 글로벌 팬덤의 지방 도시 방문 및 탐방 편의성 미흡`
- 서비스 한 줄 정의: 여러 종류의 K-콘텐츠와 연관된 비수도권 장소를 탐색하고, 이용자 조건에 맞는 로컬 여행 일정을 만들 수 있는 팬덤 기반 관광 플래너
- 핵심 대상:
  1. K-pop, 드라마, 영화, 예능, 웹툰 등 한국 콘텐츠에 관심 있는 국내외 팬
  2. 유명 수도권 관광지 밖의 콘텐츠 연관 장소를 방문하고 싶은 자유여행객
  3. 콘텐츠 장소와 주변 관광지를 한 번에 묶어 여행하고 싶은 이용자
- 핵심 가치:
  1. 콘텐츠와 실제 장소의 관계를 출처와 함께 제공한다.
  2. K-콘텐츠 장소 주변의 관광지·문화시설·음식점·숙박 등을 한국관광공사 OpenAPI로 연결한다.
  3. 여행 기간, 출발 지역, 동행자, 이동 방식, 관심 콘텐츠에 따라 지방 여행 일정을 추천한다.
  4. 한국어와 영어를 우선 지원하고 다른 언어로 확장 가능한 구조를 만든다.
- 공모전 해시태그: `#덕질투어 #다국어 #로컬관광 #여행플래너 #K콘텐츠 #지역관광`

### 2. 반드시 지켜야 할 공모전 조건

- 최종 서비스에서 한국관광공사 OpenAPI를 실제 기능에 반드시 사용한다. 로고나 문구만 표시하는 방식은 인정하지 않는다.
- 한국관광공사 API 데이터는 다음 사용자 가치에 연결한다.
  - 콘텐츠 장소 주변 관광지 검색
  - 관광지 상세 정보와 이미지 조회
  - 지역 또는 위치 기반 추천
  - 일정의 주변 관광 코스 보강
- 한국관광공사 OpenAPI는 2025년 이후 `*Service2` 계열로 변경된 내역이 있으므로, 구현 전에 한국관광 콘텐츠랩 또는 공공데이터포털의 최신 명세를 확인하고 실제 오퍼레이션명과 응답 필드를 검증하라. 기억에 의존해 엔드포인트나 필드를 만들지 마라.
- API 키는 브라우저 번들, Git 저장소, 로그에 노출하지 않는다. 서버 라우트와 환경변수를 사용한다.
- 출처가 없는 K-콘텐츠 장소, 촬영지, 아티스트 연관 정보를 AI가 임의 생성하면 안 된다.
- 외부 이미지의 저작권·공공누리 유형을 저장하고 표시할 수 있어야 한다. 이미지 사용 조건을 확인할 수 없는 경우 기본 대체 이미지를 사용한다.
- 개인정보를 MVP에 불필요하게 수집하지 않는다.
- 공모전 심사 우선순위를 개발 계획에 반영한다.
  - 서비스 구현성 30점: 과제 연계 기능 완결성, 안정성, 완성도
  - 서비스 기획력 30점: 타당성, 구체성, 독창성
  - 데이터 활용 적절성 20점: 한국관광공사 OpenAPI 실제 활용
  - 서비스 발전성 20점: 지속성, 확장성

### 3. 작업 시작 절차

1. 저장소 전체와 첨부된 UI 이미지 또는 `docs/ui-reference/` 안의 이미지를 먼저 확인하라.
2. 이미 코드가 있으면 기존 구조, 사용자 변경사항, UI 스타일을 보존하고 필요한 부분만 수정하라.
3. UI 이미지에서 다음 항목을 추출해 짧게 정리하라.
   - 화면 목록과 사용자 이동 흐름
   - 색상, 타이포그래피, 여백, 둥근 모서리, 그림자
   - 공통 컴포넌트와 반응형 변화
4. 코드 작성 전에 `docs/implementation-plan.md`에 다음 내용을 기록하라.
   - 현재 저장소 상태
   - 확인된 요구사항과 가정
   - 화면·라우트 목록
   - 데이터 모델
   - API 연동 방식
   - 단계별 구현 계획
   - 위험 요소와 검증 방법
5. 다음 항목 중 구현을 막는 정보만 한 번에 질문하라. 답이 없어도 안전한 기본값으로 진행 가능한 항목은 기본값을 명시하고 계속 진행하라.
   - 기존 UI 이미지 또는 원본 파일의 위치
   - 서비스의 확정 이름과 로고
   - 한국관광공사 OpenAPI 키 보유 여부
   - 지도 API 키 보유 여부
   - 관리자 이메일
   - Firebase 프로젝트 설정값
6. 질문에 대한 답을 기다려야 하는 핵심 비밀키가 없더라도 개발을 멈추지 마라. `.env.example`, API 어댑터, 명확히 표시된 데모 데이터로 기능을 개발하고, 키가 들어오면 실제 API로 바뀌도록 구성하라.
7. 계획만 제시하고 끝내지 말고, 현재 권한과 정보 안에서 구현·실행·검증까지 진행하라.

### 4. 권장 기술 구조

저장소에 이미 확정된 기술이 없을 때 다음을 기본값으로 사용하라. 더 적합한 선택이 있다면 이유와 교체 비용을 먼저 설명하고 승인 없이 대규모로 바꾸지 마라.

- 프론트엔드: React 기반 TypeScript 풀스택 프레임워크, Tailwind CSS
- 서버 기능: 프레임워크의 서버 라우트 또는 서버리스 함수
- 데이터베이스: Firebase Firestore
- 인증: Firebase Authentication, 관리자만 로그인
- 파일: Firebase Storage 또는 저장소의 정적 데모 이미지
- 지도: 공급자 교체가 가능한 `MapProvider` 인터페이스를 만들고, 키가 있으면 Kakao Map을 우선 연결한다.
- 배포: Vercel 또는 Firebase Hosting 중 현재 구조에 맞는 한 가지를 선택한다.
- 패키지 버전은 구현 시점의 안정 버전과 기존 lockfile을 따른다. 기존 프로젝트의 패키지 관리자를 바꾸지 않는다.

구조 예시:

```text
src/
  app/ 또는 pages/
  components/
    common/
    content/
    map/
    itinerary/
    admin/
  features/
  lib/
    tour-api/
    firebase/
    map/
    validation/
  services/
  types/
  i18n/
  mocks/
docs/
  ui-reference/
  implementation-plan.md
  data-dictionary.md
  api-usage.md
  judging-checklist.md
public/
tests/
```

실제 프레임워크 관례와 충돌하면 관례를 우선하고, 폴더를 기계적으로 복제하지 마라.

### 5. 핵심 사용자 흐름

#### 흐름 A: 콘텐츠로 장소 찾기

1. 사용자가 콘텐츠 종류 또는 작품·아티스트를 선택하거나 검색한다.
2. 지역, 콘텐츠 유형, 장소 유형, 이동 편의 등의 필터를 적용한다.
3. 결과를 카드 목록과 지도에서 확인한다.
4. 장소 상세에서 콘텐츠와의 관계, 검증 출처, 방문 팁, 위치를 확인한다.
5. 한국관광공사 OpenAPI로 받은 주변 관광지를 함께 확인한다.
6. 원하는 장소를 일정에 추가한다.

#### 흐름 B: 조건 기반 여행 일정 만들기

1. 관심 콘텐츠 또는 아티스트를 선택한다.
2. 여행 기간, 출발 지역, 동행자, 이동 수단, 선호 강도를 입력한다.
3. 조건에 맞는 비수도권 K-콘텐츠 장소를 우선 추천한다.
4. TourAPI의 주변 관광 데이터를 결합해 하루 단위 일정을 만든다.
5. 사용자는 일정 순서, 장소 추가·삭제, 체류 시간을 수정한다.
6. 일정 결과를 브라우저 로컬 저장소에 저장하고 공유용 링크 또는 인쇄 화면을 확인한다.

#### 흐름 C: 관리자 데이터 관리

1. 관리자 로그인
2. 콘텐츠, 아티스트, 콘텐츠 연관 장소를 폼으로 등록·수정
3. CSV 파일로 여러 장소를 일괄 등록하기 전에 미리보기와 행별 오류 확인
4. `임시저장 → 검수완료 → 공개` 상태 관리
5. 출처 URL, 확인일, 이미지 권리 정보를 입력하지 않으면 공개 불가

### 6. 필수 화면과 라우트

기존 UI가 있으면 시각적 구조와 서비스명을 우선 유지하되 다음 기능을 모두 수용하라.

1. `/` 홈
   - 서비스 가치 제안
   - 검색창
   - K-pop·드라마·영화·예능·웹툰 카테고리
   - 추천 로컬 콘텐츠 여행지
   - 지역별 탐색 진입점
   - 일정 만들기 CTA
2. `/explore` 탐색
   - 키워드 검색
   - 지역·콘텐츠 유형·장소 유형 필터
   - 카드/지도 보기 전환 또는 PC 2단 레이아웃
   - 정렬, 결과 수, 필터 초기화
3. `/spots/[id]` 장소 상세
   - 장소명, 주소, 좌표, 이미지
   - 연관 작품·아티스트와 관계 설명
   - 출처와 최근 확인일
   - 방문 팁과 교통 정보
   - TourAPI 기반 주변 관광지
   - 일정 추가 버튼
4. `/planner` 일정 조건 입력
5. `/planner/result` 일정 결과와 편집
6. `/saved` 로컬 저장 일정·찜 목록
7. `/admin/login` 관리자 로그인
8. `/admin` 관리자 대시보드
9. `/admin/spots` 장소 목록·검색·상태 필터
10. `/admin/spots/new`, `/admin/spots/[id]` 등록·수정
11. `/admin/import` CSV 업로드·검증·미리보기·확정
12. `not-found`, 오류, 로딩, 빈 상태 화면

### 7. 데이터 모델

TypeScript 타입, Firestore 컬렉션, 유효성 검증 스키마를 일관되게 설계하라. `docs/data-dictionary.md`에 필드명, 의미, 필수 여부, 예시를 기록하라.

#### `contents`

- `id`
- `titleKo`, `titleEn`
- `contentType`: `kpop | drama | movie | variety | webtoon | other`
- `artistNames[]`
- `summaryKo`, `summaryEn`
- `officialUrl`
- `thumbnailUrl`
- `status`: `draft | reviewed | published`
- `createdAt`, `updatedAt`

#### `spots`

- `id`
- `contentIds[]`
- `nameKo`, `nameEn`
- `regionCode`, `sigunguCode`
- `addressKo`, `addressEn`
- `latitude`, `longitude`
- `placeType`: `filming_location | hometown | exhibition | performance | store | landmark | other`
- `relationSummaryKo`, `relationSummaryEn`
- `sourceName`, `sourceUrl`, `sourceType`
- `verifiedAt`
- `visitTipsKo`, `visitTipsEn`
- `openingHours`, `feeInfo`, `transportInfo`
- `imageUrls[]`
- `imageRights[]`
- `tourApiContentId` 또는 연결 가능한 TourAPI 식별 정보
- `status`: `draft | reviewed | published`
- `createdAt`, `updatedAt`

#### `tourApiPlaces`

필요 시 API 응답을 그대로 저장하지 말고 서비스에서 실제 사용하는 정규화 필드만 캐시한다.

- `contentId`
- `contentTypeId`
- `title`
- `address`
- `latitude`, `longitude`
- `imageUrl`, `thumbnailUrl`
- `copyrightType`
- `tel`
- `sourceLanguage`
- `fetchedAt`

#### `itineraries`

MVP에서는 로그인 없는 사용자를 위해 로컬 저장을 우선한다. 서버 저장을 추가할 경우 다음 구조를 사용한다.

- `id`
- `locale`
- `tripStartDate`, `tripEndDate`
- `startRegion`
- `companionType`
- `transportMode`
- `interestContentIds[]`
- `days[]`와 각 날짜의 `items[]`
- 각 일정 항목의 `spotId`, `tourApiContentId`, `startTime`, `durationMinutes`, `order`
- `createdAt`, `updatedAt`

### 8. CSV 일괄 등록

관리자 화면에서 UTF-8 CSV를 지원하고 샘플 파일을 다운로드할 수 있게 하라. 최소 헤더는 다음과 같다.

```csv
contentTitleKo,contentTitleEn,contentType,artistNames,nameKo,nameEn,regionCode,sigunguCode,addressKo,addressEn,latitude,longitude,placeType,relationSummaryKo,relationSummaryEn,sourceName,sourceUrl,verifiedAt,visitTipsKo,visitTipsEn,imageUrl,imageRights,tourApiContentId,status
```

요구사항:

- 배열 값은 합의된 구분자(`|`)로 처리한다.
- 좌표, URL, 날짜, enum, 필수값을 검증한다.
- 동일 장소 후보를 `이름 + 주소` 또는 좌표 근접도로 경고한다.
- 실제 저장 전에 전체 행 미리보기와 성공·경고·오류 수를 보여준다.
- 오류가 있는 행 번호와 필드를 구체적으로 표시한다.
- 한 행 오류로 전체가 조용히 실패하지 않도록 한다.
- 관리자 폼과 CSV가 동일한 검증 스키마를 사용한다.

### 9. TourAPI 연동 원칙

- API 호출 코드를 한 곳에 모으고 응답을 서비스용 타입으로 정규화한다.
- 공식 최신 명세에서 확인한 지역 기반, 위치 기반, 키워드 검색, 공통 상세, 이미지 정보 기능 중 필요한 것만 사용한다.
- 언어별 서비스가 서로 다른 콘텐츠 ID나 데이터 양을 가질 수 있음을 가정하고, 한국어 결과가 없을 때의 fallback 정책을 명시한다.
- 개발 계정 호출량을 고려해 서버 캐시, 요청 중복 제거, 검색 debounce를 적용한다.
- timeout, rate limit, 빈 응답, 잘못된 키, 일시 장애를 사용자 친화적으로 처리한다.
- API 장애 시 마지막 정상 캐시 또는 명확히 표시된 데모 데이터를 보여줄 수 있지만, 실제 데이터인 것처럼 속이지 않는다.
- `docs/api-usage.md`에 다음을 기록한다.
  - 사용한 한국관광공사 API 상품명
  - 실제 사용 오퍼레이션
  - 요청 파라미터와 사용 응답 필드
  - 서비스 화면·기능과의 연결 관계
  - 캐시 정책
  - 키 설정 방법
  - 오류 처리 방식
- 심사 시 API 활용 증빙이 가능하도록 개발 모드에서 호출 상태를 확인할 수 있는 진단 정보를 제공하되 비밀키는 절대 표시하지 않는다.

### 10. 추천·일정 생성 로직

초기 버전은 설명 가능한 규칙 기반으로 구현하고, AI가 없어도 작동해야 한다.

1. 필수 조건으로 공개 상태, 지역, 날짜·운영 여부, 콘텐츠 관심사를 필터링한다.
2. 다음 요소로 후보 점수를 계산한다.
   - 관심 콘텐츠 일치도
   - 비수도권 우선도
   - 출처 검증 상태
   - 사용자 이동 방식과 거리
   - 주변 TourAPI 관광자원 밀도
   - 일정 내 경로 효율
3. 같은 유형 장소가 연속되지 않도록 다양성을 보정한다.
4. 하루 이동 거리 또는 이동 시간이 지나치게 길면 경고하고 일정 수를 줄인다.
5. 추천 카드에 `추천 이유`를 한두 문장으로 표시한다.
6. 점수 가중치는 상수 또는 설정 파일로 분리하고 테스트한다.

지도 경로 API가 없는 단계에서는 직선거리(Haversine)를 사용했다고 명확히 표시하고 실제 도로 이동시간인 것처럼 표현하지 마라.

### 11. UI·UX 기준

- 첨부된 기존 UI를 디자인의 기준으로 사용한다.
- PC 1440px에서 정보 밀도가 적절해야 하며, 1024px·768px·390px에서도 핵심 기능을 사용할 수 있어야 한다.
- 기존 UI에 없는 화면도 같은 디자인 토큰과 컴포넌트 문법으로 확장한다.
- 시맨틱 HTML, 키보드 탐색, visible focus, 레이블, 대체 텍스트, 충분한 색상 대비를 적용한다.
- 지도만으로 정보를 전달하지 말고 동일 결과를 목록으로도 제공한다.
- 모든 비동기 화면에 skeleton 또는 loading, empty, error, retry 상태를 구현한다.
- 한국어와 영어 문구를 컴포넌트에 하드코딩하지 말고 i18n 리소스로 분리한다.
- 모바일에서는 지도와 긴 필터가 화면을 막지 않도록 바텀시트나 접이식 필터를 사용한다.
- 과도한 애니메이션보다 검색·필터·일정 편집의 명확성을 우선한다.

### 12. 인증·보안·데이터 품질

- 일반 사용자 탐색과 일정 생성은 로그인 없이 가능하게 한다.
- 관리자 라우트와 쓰기 권한은 인증된 관리자만 접근 가능해야 한다.
- 클라이언트에서 관리자 이메일을 비교하는 것만으로 권한을 구현하지 않는다. Firebase custom claim 또는 서버 검증과 Firestore Security Rules를 사용한다.
- 모든 입력을 스키마로 검증하고 HTML 삽입을 피한다.
- `.env*`, 서비스 계정, API 키를 Git에 커밋하지 않는다.
- `.env.example`에는 변수 이름과 설명만 넣는다.
- Firestore 인덱스와 Security Rules도 저장소에서 관리한다.
- 공개 데이터는 `status == published`만 조회한다.
- 공개 전 `sourceUrl`, `verifiedAt`, 최소 하나의 관계 설명이 필수다.

### 13. 구현 단계

#### 1단계: 기반과 수직 슬라이스

- 프로젝트 실행 환경 정리
- 기존 UI 기반 디자인 토큰과 공통 레이아웃
- 타입과 검증 스키마
- 홈 → 탐색 → 상세 → 일정 추가의 한 흐름을 데모 데이터로 완성
- 테스트 가능한 상태 만들기

#### 2단계: 실제 관광데이터

- TourAPI 서버 프록시와 어댑터
- 주변 관광지·상세·이미지 실제 연동
- 캐시, 로딩, 오류, 빈 상태
- `docs/api-usage.md` 작성

#### 3단계: 여행 플래너

- 조건 입력
- 규칙 기반 추천과 일정 생성
- 일정 편집·저장·인쇄 또는 공유
- 추천 이유 표시

#### 4단계: 관리자

- 관리자 인증과 권한
- 콘텐츠·장소 CRUD
- CSV 템플릿, 업로드, 미리보기, 검증, 저장
- 공개 상태와 출처 검수

#### 5단계: 다국어·품질·배포

- 한국어·영어 전환
- 접근성·반응형 검토
- 테스트, lint, typecheck, production build
- 배포 문서와 심사용 데모 시나리오

각 단계가 끝날 때 실행 결과와 남은 위험을 보고하고 바로 다음 단계로 진행하라. 대규모 변경 전에는 현재 변경사항을 확인하고 사용자의 작업을 덮어쓰지 마라.

### 14. 테스트와 검증

최소한 다음을 자동 또는 수동 테스트하라.

- 검색과 필터 조합
- 장소 상세의 출처·검증일 표시
- TourAPI 정상, 빈 응답, 인증 오류, timeout
- 일정 추천의 필수 조건과 정렬 안정성
- CSV 정상 행, 필수값 누락, 잘못된 좌표·URL·날짜·enum, 중복 후보
- 일반 사용자의 관리자 접근 차단
- 공개되지 않은 데이터가 사용자 화면에 노출되지 않음
- 한국어·영어 전환 시 레이아웃과 fallback
- 1440px, 1024px, 768px, 390px 반응형
- 키보드만으로 주요 흐름 수행
- 새로고침과 직접 URL 접근

완료 전 다음 명령에 해당하는 검증을 실행하라. 실제 명령은 저장소의 패키지 스크립트를 따른다.

- lint
- typecheck
- unit/integration tests
- production build

실패를 숨기거나 테스트를 삭제해 통과시키지 마라. 외부 키 부족으로 실행하지 못한 검증은 정확한 원인과 사용자가 해야 할 한 단계를 기록하라.

### 15. 완료 조건

다음 조건을 모두 충족해야 작업 완료로 간주한다.

- 첨부 UI와 일관된 PC 중심 반응형 화면이 구현됨
- 홈 → 탐색 → 장소 상세 → 일정 생성·편집의 핵심 흐름이 실제로 작동함
- 한국관광공사 OpenAPI 데이터가 사용자에게 보이는 핵심 기능에 연결됨
- API 키가 클라이언트나 저장소에 노출되지 않음
- 관리자 로그인, CRUD, CSV 미리보기·검증·등록이 작동함
- 콘텐츠 장소마다 출처 URL과 확인일을 확인할 수 있음
- 한국어·영어 전환 구조와 기본 번역이 작동함
- 로딩·빈 상태·오류·재시도 상태가 있음
- lint, typecheck, test, production build 결과를 확인함
- `README.md`에 설치, 환경변수, 실행, 테스트, 배포 방법이 있음
- `docs/implementation-plan.md`, `docs/data-dictionary.md`, `docs/api-usage.md`, `docs/judging-checklist.md`가 최신 상태임
- 실제 구현하지 않은 기능을 README나 화면에서 구현 완료라고 표시하지 않음

### 16. 최종 보고 형식

작업이 끝나면 다음 순서로 간결하게 보고하라.

1. 구현한 사용자 가치와 핵심 흐름
2. 주요 변경 파일
3. 한국관광공사 OpenAPI가 실제로 사용된 기능
4. 관리자 데이터 등록 방법과 CSV 위치
5. 실행한 검증과 결과
6. 환경변수 또는 외부 계정 때문에 사용자가 해야 할 일
7. 공모전 심사 기준별 충족 근거
8. 아직 남은 위험과 다음 우선순위

이제 저장소와 UI 참고자료를 확인하고, 짧은 초기 진단과 구현 계획을 작성한 뒤 개발을 시작하라.

---

## Codex에 함께 제공하면 좋은 파일

- 기존 UI 화면 이미지: `docs/ui-reference/`
- 공모전 공고문 PDF: `docs/competition/`
- 팀원이 조사한 K-콘텐츠 장소 CSV: `data/import/`
- 서비스 로고 및 사용 권한이 확인된 이미지: `public/assets/`
- 기존 소스가 있다면 프로젝트 전체 폴더

## 권장 첫 실행 방식

1. VS Code에서 프로젝트 폴더를 연다.
2. UI 이미지를 `docs/ui-reference/`에 넣거나 Codex 채팅에 첨부한다.
3. 위 시작 프롬프트 전체를 새 Codex 채팅에 붙여 넣는다.
4. 처음에는 Plan 모드로 요구사항과 저장소를 점검한다.
5. 계획과 질문에 답한 뒤 코드 작업 권한으로 전환해 구현을 진행한다.
6. 같은 기능을 계속 개발할 때는 새 채팅을 만들기보다 같은 채팅에서 이어간다.

## 근거 자료

- `2026 관광데이터 활용 공모전(웹·앱 구현 부문) 공고문.pdf`
- `2026 관광데이터 활용 공모전(웹·앱 구현 부문) 지정과제 목록.pdf`
- 한국관광공사 국문 관광정보 서비스: https://www.data.go.kr/data/15101578/openapi.do
- 한국관광공사 OpenAPI 2025년 서비스 URL 변경 공지: https://www.data.go.kr/bbs/ntc/selectNotice.do?originId=NOTICE_0000000004082
- OpenAI Codex 프롬프트 권장 구성: https://learn.chatgpt.com/guides/best-practices
- OpenAI Codex `AGENTS.md` 안내: https://learn.chatgpt.com/docs/agent-configuration/agents-md
